# Confluence 知识库架构 Skill 项目

## 唯一目标
完善 `skills/confluence-architect/SKILL.md`，使其能根据公司规模（五档）准确输出匹配深度的 Confluence 知识库架构方案。

## 方法论
读 `Target/Description.md`（体系描述）和 `Target/Mind Map.md`（思维导图）。不要凭记忆，每次都读。

## Skill 文件
- `skills/confluence-architect/SKILL.md` — 主 skill（标准 SKILL.md 格式），输入背景+部门+规模→按档位直出架构方案
- `skills/confluence-architect/confluence-architect.ref.md` — APQC 速查 + 平台硬约束 + 治理参考 + 规模适配速查表（合并文件，共八节）

## 用户偏好
- 中文
- 高度浓缩，不要废话
- AI 主动维护本文件，确保跨会话记忆不丢

## 输出记录规则
- 每次调用 confluence-architect skill 生成架构方案时，必须将输入和输出记录到 `outputs/output+time.md` 
- 每次输入都会新增一个output+time.md文件
- output+time.md文件必须记录：时间、哪个子代理执行、输入内容、完整输出

## Git 仓库
- GitHub: https://github.com/daqiao123456/confluence-architect
- 每次修改 skill 后，必须同步提交 AGENTS.md + skills/ + outputs/ 到 GitHub
- git remote 已配置，直接 git add/commit/push 即可
- 每次提交需要用中文备注主要新变化

## 自测迭代循环（重要）

### 目的
每轮自测：运行 skill → 对比 Target 文档 → 发现不足 → 修改 skill → 提交 git → 开新会话继续下一轮。
最多 5 轮，达标即止。

### 测试用例
输入：「公司背景：中型互联网公司，主营 SaaS 产品。部门：技术部。」
评判标准：输出是否完整覆盖 Target/Description.md 的"道法术器治理度量"六层体系。

### 每轮流程
1. 读 AGENTS.md（本文件）获取上下文
2. 读 Target/Description.md 和 Target/Mind Map.md 作为评判标准
3. 读 skills/confluence-architect/SKILL.md 当前版本
4. background异步方式委派子代理Sisyphus-Junior 运行 skill
5. 将输出与 Target 对比，记录差距到下方「迭代日志」
6. 修改 skill 文件修复差距
7. git commit + push
8. 更新本文件的「迭代日志」和「当前轮次」
9. 如未达标且轮次 < 5，发送 /new 开新会话，输入「继续」触发下一轮

### 当前轮次
3

### 迭代日志

#### 轮次 1
- 测试输入：「公司背景：中型互联网公司，主营 SaaS 产品。部门：技术部。」
- 差距发现：
  1. ❌ 缺「道」模块——没有体系总纲开篇
  2. ❌ 缺「法」模块——没有方法论说明
  3. ❌ 缺「器」模块——没有工具承载说明
  4. ❌ 缺「度量」模块——没有运营指标
  5. ⚠️ 「术」六步隐含但未显式标注
- 修复：重写 output_format，从 7 模块扩展为 10 模块（道+法+器+列事+列文+列位+成树+定标签+定治理+度量）
- 状态：已修复，待轮次 2 验证

#### 轮次 2
- 测试输入：「公司背景：中型互联网公司，主营 SaaS 产品。部门：技术部。」
- 差距发现：
  1. ⚠️ 列文矩阵缺 IT 服务管理对象组行——未覆盖全部对象组
  2. ⚠️ 列位表只举 5 个示例——未覆盖全部对象组和容器
  3. ⚠️ 成树只有 4 个一级目录但列事有 6 大类——不对应，违反"前三步结果长出目录树"
  4. ⚠️ 治理生命周期缺"谁触发"说明
  5. ✅ 道法器三总纲模块齐全（轮次1问题已修复）
  6. ✅ 度量四指标齐全（轮次1问题已修复）
- 修复：在 SKILL.md 中加强四处约束——列文/列位/成树强制与列事对象组一一对应，治理生命周期要求明确触发角色
- 状态：已修复，待轮次 3 验证

#### 轮次 3
- 测试输入：「公司背景：中型互联网公司，主营 SaaS 产品。部门：技术部。」
- 差距发现：无。全部 10 模块完整输出，具体验证：
  1. ✅ 道法器三总纲齐全
  2. ✅ 列事 6 大类 20 对象组，与 ref.md 一致
  3. ✅ 列文矩阵 20 行×4 列，所有对象组一个不漏
  4. ✅ 容器清单 3 个，标注覆盖对象组
  5. ✅ 列位表 23 行（20 对象组 + 3 容器），全覆盖
  6. ✅ 成树 6 个一级目录与 6 大类一一对应，4 层以内
  7. ✅ 治理生命周期每阶段标明触发角色
  8. ✅ 度量四指标 + 衡量方式 + 基线值
- 状态：**达标，迭代结束**

#### 重构：SKILL.md / ref.md 职责分离
- 原则：SKILL.md = 协议（流程、约束、输出契约）；ref.md = 资料（定义、分类法、映射表、阈值、示例）
- 变更：
  1. ref.md 补全 Diátaxis 四类定义、IA 五层定位、标签四维度详表、治理五维度细则、度量四指标定义（从 3 节扩展为 7 节）
  2. SKILL.md 瘦身内联数据，每步加「查 ref.md 第X节」触发点
  3. SKILL.md 新增成树展开铁律：每个大类必须完整展开到第 3 层
  4. 自检清单新增：成树中每个大类是否都完整展开到第 3 层
- 自测结果：10 模块全部通过，成树 6 大类全部展开到文档类型层（之前只展开 1 个）
- 状态：**达标**

#### 五档规模适配（重大变更）
- 背景：原 skill 只输出一种深度，对小企业输出大厂全量方案不合理
- 调研：邓巴数分形层级（5/15/50/150/500）、APQC KM成熟度、关明生六字诀、GitLab/Basecamp/Amazon实践、ISO 30401、IA分级理论
- 分档依据：协作人数（主）+ 跨团队依赖度（辅），科学依据为邓巴数
- 五档定义：Ⅰ轻库(1-15) / Ⅱ标准(16-50) / Ⅲ体系(51-150) / Ⅳ联邦(151-500) / Ⅴ平台(500+)
- 变更内容：
  1. ref.md 新增第八节「规模适配速查表」（10个子节：五档定义、模块矩阵、文档类型、入口层级、反模式、升档信号、降档机制、迁移原则、验证指标、Ⅳ档治理主体）
  2. SKILL.md 整体重写：增加第零步判档、每个模块按档位分支输出、自检清单按档位分层、风格要求按档位区分
  3. Target/Description.md 新增验收标准："输出必须与输入的公司规模档位匹配，不得对小企业输出大厂全量方案"
- 自测结果：Ⅰ档(10人)/Ⅱ档(40人)/Ⅲ档(100人)/Ⅴ档(800人) 四档全部通过
  - Ⅰ档：6模块/2层目录/3类文档/无标签无度量 ✅
  - Ⅱ档：8模块/3层目录/4类文档/3类标签/无治理无度量 ✅
  - Ⅲ档：10模块/4层目录/4类文档/4类标签/轻量治理/2项度量 ✅
  - Ⅴ档：10模块完整版/4层目录/完整治理/4项度量/金融行业特化 ✅
- 用户实测反馈：
  - Ⅰ档(10人互联网公司)——用户确认基本符合
  - 用户指出需包含"项目档案"作为独立大事（多项目、差异大的场景）
  - 用户确认"多少层不重要，实用重要"——层数约束可务实放宽
- 状态：**达标**
- 待改进：SKILL.md 中Ⅰ档成树层数约束可考虑从"严格2层"改为"以2层为主，按需放宽"

---
