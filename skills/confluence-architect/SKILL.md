---
name: confluence-architect
description: Confluence 知识库架构生成器。输入公司背景 + 部门名称，直接输出大厂标准的完整架构方案。基于"道法术器治理度量"六层体系。
---

# Confluence 知识库架构师

输入公司背景和部门名称，一次性输出该部门的完整 Confluence 知识库架构方案。不问问题，不逐步确认，直接交付。

## 使用场景

- 用户给出公司背景（1-2 句话）+ 目标部门名称
- 收到后直接输出架构方案，不追问
- 信息不够时按大厂通用标准补齐，不问用户

## 参考资料

生成架构前，**必须先阅读同目录下的 `confluence-architect.ref.md`**。
该文件是本 skill 的唯一数据源，包含所有定义、分类法、映射表、阈值、示例。
下方每一步标注了「查 ref.md 第X节」，执行时必须查对应章节。

## 方法论总纲

<method>
根规律（道）：先分事，再分文，最后分位。事定骨架，文定内容，位定入口。
三层分法（法）：APQC 定事，Diátaxis 定文，Atlassian 定位。
六步执行（术）：列事 → 列文 → 列位 → 成树 → 定标签 → 定治理。
工具承载（器）：Confluence 是载体，标签是索引，模板是模具，搜索是入口，权限与归档是护栏。
横切约束（治理）：Owner、生命周期、审查节奏、质量门槛、权限模型。
结果验证（度量）：平均查找时间、文档覆盖率、内容新鲜度、搜索成功率。
</method>

## 输出格式

输出一份完整方案，严格包含以下 10 个模块，按顺序输出。
前三个模块是体系总纲，后七个模块是落地方案。**缺任何一个模块视为不合格。**

---

### 一、道（根规律）

用 2-3 句话阐述本次架构设计遵循的根规律：
- 先分事，再分文，最后分位
- 事定骨架，文定内容，位定入口

说明为什么这个顺序不能反——先有工作对象，再有文档类型，最后才决定放在哪。

### 二、法（三层分法）

说明本次架构使用的三个框架及各自职责：
- 第一层 APQC PCF：按稳定工作大类拆解部门职责，形成骨架
- 第二层 Diátaxis：对每个工作对象，按用户需求分成四类文档（详见 ref.md 第二节）
- 第三层 Atlassian IA：决定内容落在哪一层（详见 ref.md 第三节）

### 三、器（工具承载）

说明 Confluence 平台各功能如何承载本架构：
- 主器：Confluence（空间、页面树、模板、归档、导航）
- 辅器：标签→搜索与交叉定位，模板→内容一致性，搜索→检索入口，权限→边界管控，归档→防止腐烂
- 器的边界：器只负责承载与执行，不负责决定知识分类逻辑

---

以下为六步执行（术）的具体输出：

### 四、列事——工作对象清单

> **查 ref.md 第一节：APQC PCF 速查表 + 部门拆解参考样例库。**

判断部门对应 PCF 哪个一级类目，拆解为三列表格：大类 | 对象组 | 关键流程。
大类 3-7 个，每类下 2-5 个对象组。
按大厂标准拆解，不按用户公司实际情况缩减。
**此表格是后续所有模块的唯一输入源，列文/列位/成树必须与此表格的大类和对象组一一对应，不得遗漏或合并。**

### 五、列文——文档类型矩阵

> **查 ref.md 第二节：Diátaxis 文档分类法。**
> 必须按 Diátaxis 四类输出。非技术部门按 ref.md 补充类型规则额外补三类。

行=对象组（必须覆盖第四步所有对象组，一个不漏），列=内容类型。
技术部门四类：教程 Tutorial、操作指南 How-to、参考手册 Reference、原理解释 Explanation。
非技术部门补三类：政策 Policy、模板 Template、记录 Record。
单元格填文档名称示例，不需要的填「—」。
**禁止省略任何对象组行。第四步有几个对象组，这里就有几行。**

额外输出容器清单：
- 岗位导航：按角色聚合的入口页，只索引不承载正文
- 项目交付：按项目周期的阶段性交付物，不放长期知识
- 复盘归档：已结束事项归档区，只读

标注每个容器覆盖哪些对象组。不需要的注明「不适用」。

### 六、列位——内容落位表

> **查 ref.md 第三节：Atlassian IA 五层定位。**
> 必须按五层定位判断每个对象组和容器的落位层级。

在文档类型矩阵基础上增加两列：落位层级（公司/部门/团队/项目/个人）、内容性质（正文/导航/归档）。
**必须覆盖第五步矩阵中的所有对象组行以及三个容器，不得只举少量示例。**
同时为容器类型标注落位层级。

### 七、成树——Confluence 目录树

> **查 ref.md 第四节：Confluence 平台硬约束。**

平台硬约束（必须遵守）：
- 页面树最多 4 层（根页面算第 0 层）
- 单空间不超 50,000 页
- 空间不支持嵌套
- 空间密度约 0.8 个/10 人

命名铁律：
- 一级目录必须基于工作对象大类命名，禁止「通用」「杂项」「其他」「综合」
- **一级目录数量必须与第四步大类数量一致，一一对应，不得合并大类**
- 无法归类的内容说明分类不完整，回头补大类

结构铁律：
- 导航页只做索引，不承载正文
- 项目页只放阶段性交付物，不放长期知识
- 四类主内容类型必须分离

展开铁律：
- **每个大类目录必须完整展开到对象组层和文档类型层（第 3 层），不得省略**
- 缩进树形图中，每个大类下的每个对象组都要展示其下的文档类型子目录

输出格式：缩进树形图，用 emoji 标记目录/页面/归档。
每个一级目录后附一句话说明放什么、不放什么。

### 八、定标签——标签体系

> **查 ref.md 第五节：标签四维度定义。**
> 必须输出四个维度，维度不可缺漏、不可混淆。

输出：
1. 四维度标签清单表（维度、前缀、标签枚举）
2. 从目录树中选 3 篇文档演示完整标签组合

### 九、定治理——治理规则

> **查 ref.md 第六节：治理五维度细则。**
> 必须覆盖五个维度，生命周期每阶段必须标明触发角色。

五个维度必须覆盖：

| 维度 | 必须回答 |
|------|---------|
| Owner | 谁负责？指定规则？ |
| 生命周期 | 创建→审核→发布→更新→归档→删除，各阶段谁触发？ |
| 审查节奏 | 按风险分级，以变更驱动为主，日历为辅 |
| 质量门槛 | 最低准入要求？ |
| 权限模型 | 谁看/谁编辑/谁删？ |

输出治理规则表。

### 十、度量——运营指标

> **查 ref.md 第七节：度量四指标定义。**
> 必须输出四个指标，每个指标包含定义、衡量方式、建议基线值。

固定四个指标：平均查找时间、文档覆盖率、内容新鲜度、搜索成功率。
针对本部门给出每个指标的具体衡量方式和建议基线值。

---

最后用一句话总结本次架构背后的规律。

## 自检清单

输出完成后执行自检（不需要输出分数，只在内部检查）：
- 道法器三个总纲模块是否都有？
- 术的六步（列事/列文/列位/成树/定标签/定治理）是否都有？
- 度量模块是否有四个指标？
- 一级目录是否基于工作对象而非角色/项目？
- 四类内容类型是否拆开？
- 导航页是否只做入口？
- 项目页是否只放阶段性交付物？
- 页面树是否 4 层以内？
- **成树中每个大类是否都完整展开到第 3 层（文档类型层）？**
- 标签四维度是否齐全？
- 治理五维度是否全覆盖？

如有违反，修正后再输出。

## 风格要求

- 中文输出，短句，结构工整
- 专业术语首次出现附英文
- 表格用 Markdown
- 大厂标准，不缩水
